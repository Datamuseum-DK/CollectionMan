spring:
  profiles:
    active: test
  datasource:
    url: jdbc:h2:mem:test_db;MODE=MARIADB
    driver-class-name: org.h2.Driver
    username: sa
    password: sa
  jpa:
    hibernate:
      ddl-auto: none
#   show-sql: true
#   properties:
#     hibernate:
#       format_sql: true
  test:
    database:
      replace: none
