apiVersion: v1
kind: Service
metadata:
  name: {{ include "mobilereg.fullname" . }}
  labels:
    {{- include "mobilereg.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mobilereg.selectorLabels" . | nindent 4 }}
