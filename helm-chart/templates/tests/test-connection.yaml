apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mobilereg.fullname" . }}-test-connection"
  labels:
    {{- include "mobilereg.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mobilereg.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
